# ============================================================
#  config.py  —  всі налаштування в одному місці
# ============================================================

# Налаштування Telegram (змінні середовища для Railway)
import os

# Налаштування Telegram (змінні середовища для Railway)
TELEGRAM_TOKEN   = os.getenv("TELEGRAM_TOKEN", "")
TELEGRAM_CHAT_ID = os.getenv("TELEGRAM_CHAT_ID", "")
 

# --- Торговий символ ---
SYMBOL = "BTCUSDT"

# --- Таймфрейми для аналізу ---
TIMEFRAMES = ["5m", "15m", "30m", "1h"]

# --- Скільки свічок завантажувати для розрахунку індикаторів ---
CANDLES_LIMIT = 200

# --- Мінімальний сигнальний скор (з 6 можливих балів) ---
# 4+ = лише якісні сигнали (рекомендовано)
# 3  = більше сигналів, але гірша якість
MIN_SCORE = 4

# --- Кулдаун між сигналами на одному таймфреймі (хвилини) ---
SIGNAL_COOLDOWN = {
    "5m":  20,
    "15m": 45,
    "30m": 90,
    "1h":  180,
}

# --- Порти веб-дашборду ---
WEB_PORT = 5000

# --- Інтервал перевірки сигналів (секунди) ---
CHECK_INTERVAL = 60  # кожну хвилину
