# config.py — всі налаштування
import os

# ── Telegram ──────────────────────────────────────────────────
TELEGRAM_TOKEN   = os.getenv("TELEGRAM_TOKEN", "")
TELEGRAM_CHAT_ID = os.getenv("TELEGRAM_CHAT_ID", "")

# ── Торговий символ ───────────────────────────────────────────
SYMBOL = "BTCUSDT"

# ── Таймфрейми для КЛАСИЧНОЇ стратегії ───────────────────────
TIMEFRAMES = ["5m", "15m", "30m", "1h"]

# ── Krajekis моніторить тільки 15м ───────────────────────────
KRAJEKIS_TIMEFRAME = "15m"

# ── Кількість свічок ──────────────────────────────────────────
CANDLES_LIMIT = 200

# ── Мінімальний скор для класичної стратегії (макс 6) ────────
MIN_SCORE = 4

# ── Кулдаун між класичними сигналами (хвилини) ───────────────
SIGNAL_COOLDOWN = {
    "5m":  20,
    "15m": 45,
    "30m": 90,
    "1h":  180,
}

# ── Інтервали перевірки (секунди) ────────────────────────────
CHECK_INTERVAL_CLASSIC  = 60    # класична — кожну хвилину
CHECK_INTERVAL_KRAJEKIS = 60    # krajekis — кожну хвилину

# ── Порт дашборду ─────────────────────────────────────────────
WEB_PORT = int(os.getenv("PORT", 5000))
